@page "/"
@inject NavigationManager Nav
@inject TokenService TokenService
@using Pdmt.Client.Services

@code {
    protected override void OnInitialized()
    {
        Nav.NavigateTo(TokenService.IsAuthenticated ? "/events" : "/login");
    }
}
